# Chat Room App
App will be runnign on defualt port 3000.
To access app use http://localhost:3000

## Create .env file to define environment veriables
Copy `.env.sample` to `.env` before starting the app.
```
cp .env.sample .env
```
